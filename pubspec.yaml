name: para_ti
description: "App romántica hecha en Flutter para San Valentín 💕"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Iconos estilo iOS
  cupertino_icons: ^1.0.8

  # Tipografías bonitas
  google_fonts: ^6.1.0

  # Guardar datos locales (si luego quieres persistir cosas)
  shared_preferences: ^2.2.2

  # Firebase (notas compartidas)
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.0
  audioplayers: ^6.1.0
  lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
